# Go GitHub API Fetcher

This simple Go program makes an HTTP GET request to the GitHub API and prints the response data.

## How to Run Locally

1. **Clone the Repository:**
   ```bash
   git clone https://github.com/ChanchalS7/fetch-api-golang.git
   cd fetch-api-golang
